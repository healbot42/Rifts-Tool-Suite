import packageMetadata from '../../package.json'

export const APP_NAME = 'Rifts Tool Suite'
export const APP_VERSION = packageMetadata.version
export const APP_RELEASE_LABEL = `${APP_NAME} v${APP_VERSION}`
